<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Generate API Key</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Roboto+Mono:wght@400;500&display=swap" rel="stylesheet">
  
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Navbar styling */
    .navbar {
      background-color: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }
    .brand-logo {
      font-weight: 600;
      color: #333;
      text-decoration: none;
      font-size: 1.2rem;
    }

    /* Card styling */
    .main-card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      background: #ffffff;
    }

    .card-header-custom {
      background: linear-gradient(to right, #4e54c8, #8f94fb); /* Warna Ungu-Biru Modern */
      color: white;
      padding: 2rem;
      text-align: center;
      border-bottom: none;
    }

    .form-control {
      border-radius: 10px;
      padding: 12px;
      border: 1px solid #e1e1e1;
      background-color: #fcfcfc;
    }
    .form-control:focus {
      box-shadow: 0 0 0 4px rgba(78, 84, 200, 0.1);
      border-color: #4e54c8;
    }

    .form-label {
      font-weight: 500;
      color: #555;
      margin-bottom: 8px;
    }

    /* API Key Box Styling (Tech look) */
    .api-key-container {
      background-color: #2d3436;
      border-radius: 12px;
      padding: 1.5rem;
      position: relative;
      margin-bottom: 1.5rem;
      border: 1px solid #444;
    }
    
    .api-key-label {
      color: #b2bec3;
      font-size: 0.85rem;
      margin-bottom: 0.5rem;
      display: block;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .api-key-box {
      font-family: 'Roboto Mono', monospace;
      color: #00cec9; /* Warna cyan neon */
      font-size: 1.1rem;
      word-break: break-all;
      letter-spacing: 0.5px;
    }

    /* Buttons */
    .btn-generate {
      background-color: #dfe6e9;
      color: #2d3436;
      font-weight: 600;
      border: none;
      padding: 12px 24px;
      border-radius: 10px;
      transition: all 0.3s ease;
    }
    .btn-generate:hover {
      background-color: #b2bec3;
      transform: translateY(-2px);
    }

    .btn-save {
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: white;
      font-weight: 600;
      border: none;
      padding: 12px 24px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(78, 84, 200, 0.3);
      transition: all 0.3s ease;
    }
    .btn-save:hover {
      background: linear-gradient(to right, #3f44a5, #7a7fe0);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(78, 84, 200, 0.4);
    }

    .btn-admin {
      background-color: #2d3436;
      color: white;
      border-radius: 50px;
      padding: 8px 20px;
      font-size: 0.9rem;
      transition: all 0.2s;
    }
    .btn-admin:hover {
      background-color: #000;
      color: #fff;
    }

  </style>
</head>
<body>

  <nav class="navbar fixed-top">
    <div class="container">
      <span class="brand-logo">🔑 API Manager</span>
      <a href="/admin/login" class="btn btn-admin shadow-sm">
        Login Admin
      </a>
    </div>
  </nav>

  <div class="container d-flex align-items-center justify-content-center flex-grow-1 mt-5 pt-5 mb-5">
    <div class="col-md-8 col-lg-6">

      <div class="card main-card">
        
        <div class="card-header-custom">
          <h2 class="mb-0 fw-bold">Dapatkan API Key</h2>
          <p class="mb-0 opacity-75 small">Isi data diri Anda untuk memulai</p>
        </div>

        <div class="card-body p-4 p-md-5">

          <% if (message) { %>
            <div class="alert alert-<%= message.type === 'success' ? 'success' : 'danger' %> border-0 shadow-sm mb-4" role="alert">
              <% if(message.type === 'success') { %> ✅ <% } else { %> ⚠️ <% } %>
              <%= message.text %>
            </div>
          <% } %>

          <form action="/save-key" method="POST" id="apiKeyForm">

            <div class="row mb-3">
              <div class="col-md-6 mb-3 mb-md-0">
                <label for="first_name" class="form-label">First Name</label>
                <input type="text" class="form-control" id="first_name" name="first_name" placeholder="your first name" required>
              </div>
              <div class="col-md-6">
                <label for="last_name" class="form-label">Last Name</label>
                <input type="text" class="form-control" id="last_name" name="last_name" placeholder="your last name" required>
              </div>
            </div>

            <div class="mb-4">
              <label for="email" class="form-label">Email Address</label>
              <input type="email" class="form-control" id="email" name="email" placeholder="your email address" required>
            </div>

            <div class="api-key-container">
              <label class="api-key-label">Generated Key:</label>
              <input type="hidden" id="apiKeyInput" name="api_key">
              <div class="api-key-box" id="apiKeyDisplay">
                Klik "Generate" &darr;
              </div>
            </div>

            <div class="d-grid gap-3 d-md-flex justify-content-between align-items-center">
              <button type="button" class="btn btn-generate w-100" id="generateBtn">
                Generate Key
              </button>
              <button type="submit" class="btn btn-save w-100">
                Simpan & Aktifkan
              </button>
            </div>

          </form>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById('generateBtn').addEventListener('click', function() {
      const btn = this;
      const originalText = btn.innerHTML;
      
      btn.disabled = true;
      btn.innerHTML = '🔄 Generating...';

      fetch('/generate-key')
        .then(response => response.json())
        .then(data => {
          document.getElementById('apiKeyInput').value = data.apiKey;
          // Efek mengetik sederhana atau langsung tampil
          document.getElementById('apiKeyDisplay').textContent = data.apiKey;
          document.getElementById('apiKeyDisplay').style.color = '#55efc4'; // Ubah warna jadi hijau terang saat ada key
          
          btn.disabled = false;
          btn.innerHTML = originalText;
        })
        .catch(err => {
          console.error(err);
          document.getElementById('apiKeyDisplay').textContent = 'Error. Coba lagi.';
          document.getElementById('apiKeyDisplay').style.color = '#ff7675';
          btn.disabled = false;
          btn.innerHTML = originalText;
        });
    });
  </script>

</body>
</html>