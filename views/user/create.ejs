<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Generate API Key</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Roboto+Mono:wght@400;500&display=swap" rel="stylesheet">
  
  <style>
    /* Agar pas 1 layar dan tidak scroll */
    html, body {
      height: 100%;
      margin: 0;
      overflow: hidden; /* Mencegah scrollbar muncul */
    }

    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      font-family: 'Poppins', sans-serif;
    }

    /* Navbar styling yang lebih tipis */
    .navbar {
      background-color: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      height: 60px; /* Tinggi navbar fix */
      border-bottom: 1px solid rgba(0,0,0,0.05);
    }
    
    .brand-logo {
      font-weight: 600;
      color: #333;
      text-decoration: none;
      font-size: 1.1rem;
    }

    /* Wrapper utama untuk menengahkan Card */
    .main-wrapper {
      height: 100vh; /* Full tinggi layar */
      display: flex;
      align-items: center;
      justify-content: center;
      padding-top: 60px; /* Kompensasi tinggi navbar */
    }

    /* Card styling yang lebih compact */
    .main-card {
      border: none;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
      background: #ffffff;
      width: 100%;
      max-width: 500px; /* Membatasi lebar agar tidak terlalu lebar */
    }

    .card-header-custom {
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: white;
      padding: 1rem; /* Padding dikecilkan */
      text-align: center;
      border-radius: 15px 15px 0 0;
    }

    .card-body {
      padding: 1.5rem; /* Padding body dikecilkan */
    }

    /* Form control lebih tipis */
    .form-control {
      font-size: 0.9rem;
      padding: 8px 12px;
      border-radius: 8px;
      background-color: #fcfcfc;
    }
    .form-label {
      font-size: 0.85rem;
      font-weight: 500;
      color: #555;
      margin-bottom: 4px;
    }

    /* API Key Box Compact */
    .api-key-container {
      background-color: #2d3436;
      border-radius: 10px;
      padding: 1rem;
      margin-top: 1rem;
      margin-bottom: 1rem;
      border: 1px solid #444;
    }
    
    .api-key-label {
      color: #b2bec3;
      font-size: 0.75rem;
      margin-bottom: 0.2rem;
      display: block;
      text-transform: uppercase;
    }

    .api-key-box {
      font-family: 'Roboto Mono', monospace;
      color: #00cec9;
      font-size: 0.95rem;
      word-break: break-all;
    }

    /* Buttons */
    .btn {
      font-size: 0.9rem;
      padding: 8px 16px;
      border-radius: 8px;
    }
    
    .btn-generate {
      background-color: #dfe6e9;
      color: #2d3436;
      font-weight: 600;
      border: none;
    }
    .btn-generate:hover { background-color: #b2bec3; }

    .btn-save {
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: white;
      font-weight: 600;
      border: none;
    }
    .btn-save:hover { background: linear-gradient(to right, #3f44a5, #7a7fe0); }

    .btn-admin {
      background-color: #2d3436;
      color: white;
      border-radius: 50px;
      font-size: 0.8rem;
    }
    .btn-admin:hover { background-color: #000; color: white; }

    /* Alert Compact */
    .alert {
      padding: 0.5rem 1rem;
      font-size: 0.85rem;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>

  <nav class="navbar fixed-top">
    <div class="container">
      <span class="brand-logo">🔑 APIKey Generator</span>
      <a href="/admin/login" class="btn btn-admin shadow-sm">
        Login Admin
      </a>
    </div>
  </nav>

  <div class="main-wrapper">
    <div class="container d-flex justify-content-center">
      
      <div class="card main-card">
        
        <div class="card-header-custom">
          <h5 class="mb-0 fw-bold">Dapatkan API Key</h5>
          <small class="opacity-75" style="font-size: 0.75rem;">Isi data untuk memulai</small>
        </div>

        <div class="card-body">

          <% if (message) { %>
            <div class="alert alert-<%= message.type === 'success' ? 'success' : 'danger' %> border-0 shadow-sm" role="alert">
              <% if(message.type === 'success') { %> ✅ <% } else { %> ⚠️ <% } %>
              <%= message.text %>
            </div>
          <% } %>

          <form action="/save-key" method="POST" id="apiKeyForm">

            <div class="row mb-2">
              <div class="col-6">
                <label for="first_name" class="form-label">First Name</label>
                <input type="text" class="form-control" id="first_name" name="first_name" placeholder="your first name" required>
              </div>
              <div class="col-6">
                <label for="last_name" class="form-label">Last Name</label>
                <input type="text" class="form-control" id="last_name" name="last_name" placeholder="your last name" required>
              </div>
            </div>

            <div class="mb-3">
              <label for="email" class="form-label">Email Address</label>
              <input type="email" class="form-control" id="email" name="email" placeholder="your email address" required>
            </div>

            <div class="api-key-container">
              <label class="api-key-label">Generated Key:</label>
              <input type="hidden" id="apiKeyInput" name="api_key">
              <div class="api-key-box" id="apiKeyDisplay">
                Klik "Generate" &darr;
              </div>
            </div>

            <div class="d-grid gap-2 d-flex justify-content-between">
              <button type="button" class="btn btn-generate w-50" id="generateBtn">
                ⚡ Generate
              </button>
              <button type="submit" class="btn btn-save w-50">
                💾 Save
              </button>
            </div>

          </form>
        </div>
      </div>
      </div>
  </div>

  <script>
    document.getElementById('generateBtn').addEventListener('click', function() {
      const btn = this;
      const originalText = btn.innerHTML;
      btn.disabled = true;
      btn.innerHTML = '🔄 ...';

      fetch('/generate-key')
        .then(response => response.json())
        .then(data => {
          document.getElementById('apiKeyInput').value = data.apiKey;
          document.getElementById('apiKeyDisplay').textContent = data.apiKey;
          document.getElementById('apiKeyDisplay').style.color = '#55efc4';
          btn.disabled = false;
          btn.innerHTML = originalText;
        })
        .catch(err => {
          console.error(err);
          document.getElementById('apiKeyDisplay').textContent = 'Error.';
          document.getElementById('apiKeyDisplay').style.color = '#ff7675';
          btn.disabled = false;
          btn.innerHTML = originalText;
        });
    });
  </script>

</body>
</html>