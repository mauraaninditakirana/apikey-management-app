<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  
  <style>
    body {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      font-family: 'Poppins', sans-serif;
      min-height: 100vh;
      padding-bottom: 2rem; /* Ruang untuk scroll bawah */
    }

    /* Navbar Styling */
    .navbar {
      background-color: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      box-shadow: 0 2px 15px rgba(0,0,0,0.05);
      height: 70px;
    }
    .navbar-brand {
      font-weight: 600;
      color: #333 !important;
      font-size: 1.2rem;
    }
    .btn-logout {
      background-color: #ff7675;
      color: white;
      border: none;
      border-radius: 50px;
      padding: 8px 24px;
      font-size: 0.9rem;
      font-weight: 500;
      transition: all 0.3s;
    }
    .btn-logout:hover {
      background-color: #d63031;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(214, 48, 49, 0.3);
    }

    /* Container Utama */
    .dashboard-container {
      margin-top: 2rem;
    }

    /* Card Table Styling */
    .table-card {
      border: none;
      border-radius: 15px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
      overflow: hidden; /* Agar sudut rounded tabel tidak bocor */
      background: white;
    }

    .card-header-custom {
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: white;
      padding: 1.5rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    /* Table Styling */
    .table {
      margin-bottom: 0;
    }
    .table thead th {
      background-color: #f8f9fa;
      color: #636e72;
      font-weight: 600;
      font-size: 0.85rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      border-bottom: 2px solid #e9ecef;
      padding: 1rem;
    }
    .table tbody td {
      padding: 1rem;
      vertical-align: middle;
      color: #2d3436;
      font-size: 0.95rem;
      border-bottom: 1px solid #f1f2f6;
    }
    .table tbody tr:hover {
      background-color: #fcfcfc;
    }
    .table tbody tr:last-child td {
      border-bottom: none;
    }

    /* Badges */
    .status-badge {
      padding: 6px 12px;
      border-radius: 50px;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
    }
    .status-valid {
      background-color: #e3f9e5; /* Hijau muda lembut */
      color: #27ae60;
    }
    .status-invalid {
      background-color: #ffeaea; /* Merah muda lembut */
      color: #e15f41;
    }

    /* Action Button */
    .btn-detail {
      background: linear-gradient(to right, #4e54c8, #8f94fb);
      color: white;
      border: none;
      border-radius: 8px;
      padding: 6px 16px;
      font-size: 0.85rem;
      transition: all 0.2s;
      text-decoration: none;
      display: inline-block;
    }
    .btn-detail:hover {
      background: linear-gradient(to right, #3f44a5, #7a7fe0);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(78, 84, 200, 0.3);
    }

    /* Responsiveness */
    @media (max-width: 768px) {
      .card-header-custom {
        flex-direction: column;
        align-items: flex-start;
      }
      .card-header-custom h4 {
        margin-bottom: 0.5rem;
      }
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container">
      <a class="navbar-brand" href="#">
        🚀 Admin Dashboard
      </a>
      <a href="/admin/logout" class="btn btn-logout">
        Logout
      </a>
    </div>
  </nav>

  <div style="height: 90px;"></div>

  <div class="container dashboard-container">
    
    <div class="card table-card">
      
      <div class="card-header-custom">
        <h4 class="mb-0 fw-bold">Daftar API Keys</h4>
        <span class="badge bg-white text-dark shadow-sm">Total: <%= keys.length %></span>
      </div>

      <div class="card-body p-0">
        <div class="table-responsive">
          <table class="table align-middle">
            <thead>
              <tr>
                <th scope="col" class="ps-4">Nama Lengkap</th>
                <th scope="col">Email</th>
                <th scope="col">Status</th>
                <th scope="col">End Date</th>
                <th scope="col" class="text-end pe-4">Aksi</th>
              </tr>
            </thead>
            <tbody>
              
              <% keys.forEach(key => { %>
                <tr>
                  <td class="ps-4 fw-medium">
                    <%= key.first_name %> <%= key.last_name %>
                  </td>
                  <td class="text-muted"><%= key.email %></td>
                  <td>
                    <% if (key.status === 'valid') { %>
                      <span class="status-badge status-valid">Valid</span>
                    <% } else { %>
                      <span class="status-badge status-invalid">Invalid</span>
                    <% } %>
                  </td>
                  <td>
                    <%= new Date(key.end_date).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                  </td>
                  <td class="text-end pe-4">
                    <a href="/admin/detail/<%= key.id %>" class="btn-detail">
                      Lihat Detail
                    </a>
                  </td>
                </tr>
              <% }) %>

              <% if (keys.length === 0) { %>
                <tr>
                  <td colspan="5" class="text-center py-5 text-muted">
                    <img src="https://cdn-icons-png.flaticon.com/512/7486/7486776.png" width="60" alt="empty" class="mb-3 opacity-50">
                    <br>
                    Belum ada API Key yang dibuat.
                  </td>
                </tr>
              <% } %>

            </tbody>
          </table>
        </div>
      </div>
      </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>